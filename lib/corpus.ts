import fs from 'node:fs/promises';import path from 'node:path';import matter from 'gray-matter';import AdmZip from 'adm-zip';
const root=process.cwd();const excluded=new Set(['README.md']);export type CorpusDoc={path:string;slug:string;title:string;documentId?:string;status:string;body:string;frontmatter:Record<string,unknown>};
export async function listCorpus():Promise<CorpusDoc[]>{const files=(await fs.readdir(root)).filter(f=>f.endsWith('.md')&&!excluded.has(f));const parse=(file:string,raw:string,status:string):CorpusDoc=>{const parsed=matter(raw);const data=parsed.data as Record<string,unknown>;return {path:file,slug:String(data.slug||file.replace(/\.md$/,'')),title:String(data.title||parsed.content.match(/^#\s+(.+)$/m)?.[1]||file),documentId:data.document_id?String(data.document_id):undefined,status,body:parsed.content,frontmatter:data}};const docs=await Promise.all(files.map(async file=>parse(file,await fs.readFile(path.join(root,file),'utf8'),file.startsWith('compass_')?'legacy':file.startsWith('00_')?'methodology':'canonical')));try{const zip=new AdmZip(path.join(root,'galgos research.zip'));for(const entry of zip.getEntries().filter(e=>e.entryName.endsWith('.md'))){const doc=parse(`zip:${entry.entryName}`,entry.getData().toString('utf8'),'canonical');if(!docs.some(existing=>existing.documentId&&existing.documentId===doc.documentId))docs.push(doc)}}catch{}return docs.sort((a,b)=>a.path.localeCompare(b.path))}export async function getCorpusDocument(slug:string){return (await listCorpus()).find(d=>d.slug===slug)}
