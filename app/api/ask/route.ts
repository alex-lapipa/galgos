import {NextResponse} from 'next/server';import {z} from 'zod';const request=z.object({question:z.string().min(8).max(1200)});export async function POST(req:Request){const parsed=request.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'A research question is required.'},{status:400});return NextResponse.json({status:'not-configured',question:parsed.data.question,message:'Ask the Archive is intentionally disabled until hybrid retrieval and the approved AI Gateway model are configured on the preview branch.'},{status:503})}
