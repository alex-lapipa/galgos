import {listCorpus} from '@/lib/corpus';import Link from 'next/link';export default async function Archive(){const docs=await listCorpus();return <main className="page"><span className="eyebrow">Research document archive</span><h1>The record.</h1><section className="grid">{docs.map(d=><Link className="panel" href={`/archive/${d.slug}`} style={{gridColumn:'span 6',textDecoration:'none',color:'inherit'}} key={d.path}><span className="mono">{d.documentId||'UNREGISTERED'} · {d.status}</span><h2>{d.title}</h2><p>{d.path}</p></Link>)}</section></main>}
