import Link from 'next/link';import {chapters} from '@/lib/content';export default function Journey(){return <main className="page"><span className="eyebrow">Chronological navigation</span><h1>The journey</h1><section className="grid">{chapters.map(([slug,num,title,summary])=><Link className="panel" style={{gridColumn:'span 6',textDecoration:'none',color:'inherit'}} href={`/era/${slug}`} key={slug}><span className="mono">Chapter {num}</span><h2>{title}</h2><p>{summary}</p></Link>)}</section></main>}
