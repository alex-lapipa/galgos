import {chapters} from '@/lib/content';import {notFound} from 'next/navigation';export function generateStaticParams(){return chapters.map(([slug])=>({slug}))}export default async function Era({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=chapters.find(x=>x[0]===slug);if(!c)notFound();return <main className="page"><span className="eyebrow">Chapter {c[1]}</span><h1>{c[2]}</h1><p style={{maxWidth:640,fontSize:20,lineHeight:1.5}}>{c[3]}</p><section className="panel" style={{marginTop:48}}><span className="evidence">Research status: researching</span><p>Chapter exhibits activate from canonical corpus records after preview ingestion. Draft and legacy material remains visible but is never presented as complete research.</p></section></main>}
