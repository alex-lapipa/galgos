import Link from 'next/link'; import {chapters} from '@/lib/content'; import {GalgoSilhouette} from '@/components/GalgoSilhouette';
export default function Home(){return <main><section className="hero"><span className="eyebrow">A research archive / evidence engine / cultural artifact</span><div className="hero-mark"><GalgoSilhouette className="hero-galgo"/></div><h1>GALGO<br/>/7</h1><p>Long-legged sight-hunting dogs recur across human history. This archive follows the evidence — and its limits.</p><Link className="mono" href="/journey">Enter the journey →</Link></section><div className="spectrum">{Array.from({length:7},(_,i)=><i key={i}/>)}</div><section className="grid">{chapters.map(([slug,num,title,summary],i)=><Link key={slug} href={`/era/${slug}`} className="panel" style={{gridColumn:'span 4',color:`var(--${['verm','amber','yellow','green','cyan','cobalt','uv'][i]})`,textDecoration:'none'}}><span className="mono">{num}</span><h2>{title}</h2><p>{summary}</p></Link>)}</section></main>}
